<% content_for :title, "New event" %>

<h1>New event</h1>

<div class="notification is-info is-light has-text-centered mb-4">
  Today is <%= Time.zone.now.strftime("%A, %B %-d, %Y") %>
</div>

<%= render "form", event: @event %>

<br>

<div>
  <%= link_to "Back to events", events_path %>
</div>
